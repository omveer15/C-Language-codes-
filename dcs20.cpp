#include <stdio.h>
    int main()
    { int a = 4;
        switch (a) {
		default:
			printf("DEFAULT");
		case 1:
			printf("ONE");
		case 2:
			printf("TWO");
		case 3:
			printf("THREE");
        }
    }

